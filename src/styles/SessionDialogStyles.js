const styles = theme => ({
	container: {
		display: 'flex',
		flexWrap: 'wrap',
	},
	formControl: {
		margin: theme.spacing(1),
		minWidth: 120,
  },
  button: {
    paddingLeft: '.5rem'
  },
  icon: {
    height: '1rem'
  }
});

export default styles;